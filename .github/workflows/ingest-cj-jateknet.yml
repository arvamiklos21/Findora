name: Ingest CJ Jateknet feed

on:
  workflow_dispatch:
  schedule:
    - cron: "22 7,17 * * *"   # 08:22 és 18:22 HU idő körül

permissions:
  contents: write

jobs:
  ingest-jateknet:
    runs-on: ubuntu-latest

    env:
      CJ_FEED_URL: ${{ secrets.CJ_FEED_URL }}
      CJ_HTTP_USER: ${{ secrets.CJ_HTTP_USER }}
      CJ_HTTP_PASS: ${{ secrets.CJ_HTTP_PASS }}
      CJ_API_TOKEN: ${{ secrets.CJ_API_TOKEN }}

    steps:
      - name: Checkout (full)
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          set -e
          pip install --upgrade pip
          pip install requests

      - name: Build CJ Jateknet feed
        run: |
          set -e
          echo "=== CJ Jateknet build indul ==="
          python scripts/build_cj_jateknet.py
          echo "=== CJ Jateknet build kész, mappa tartalom: ==="
          ls -R docs/feeds/cj-jateknet || echo "Nincs docs/feeds/cj-jateknet mappa"

      - name: Commit & push Jateknet feed
        run: |
          set -e
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

          git fetch origin
          git checkout main
          git reset --hard origin/main

          git add docs/feeds/cj-jateknet || true

          if git diff --cached --quiet; then
            echo "No changes to commit."
          else
            git commit -m "CJ Jateknet feed update"
            git push origin HEAD:main
          fi
